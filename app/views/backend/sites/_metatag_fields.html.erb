<div class="nested-fields">
  &lt;meta name="<%= f.select(:name, suggested_site_metatags, prompt: '') %>" content="<%= f.text_field :content %>" /&gt;

  <%= link_to_remove_association(t(:remove_metatag), f,
                                 class: 'btn btn-outline-danger', role: 'button') %>
</div>
