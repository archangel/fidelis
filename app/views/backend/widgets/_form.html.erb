<%= simple_form_for([:backend, widget], html: { autocomplete: 'false' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :slug %>
  <%= f.association :design, prompt: :translate %>
  <%= f.input :content %>

  <%= f.button :submit %>
<% end %>
